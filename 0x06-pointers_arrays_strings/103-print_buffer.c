#include "holberton.h"

/**
 * print_buffer - This program will print using _putchar function
 * @b: character
 * @size: number
 * Description: prints Holberton using the function
 * Return: s
 *
 */

void print_buffer(char *b, int size)
{
int d = 0, b, e = n;
int *c;
for (d = 0; d < n; d++)
{
c[d] = a[d];
}

for (b = 0; b < n; b++)
{
a[b] = c[e - 1];
e = e - 1;
}
}
