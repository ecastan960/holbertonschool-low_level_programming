#include "holberton.h"

/**
 * _strstr - This program will print using _putchar function
 * @haystack: array
 * @needle: number
 * Description: prints Holberton using the function
 * Return: char
 *
 */

char *_strstr(char *haystack, char *needle)
{
while (*s != c)
{
if (*s != 0)
{
s++;
}
else
{
s = 0;
return (s);
}
}
return (s);
}
