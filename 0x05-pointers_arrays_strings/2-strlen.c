#include "holberton.h"

/**
 * _strlen - This program will print using _putchar function
 * @s: string
 * Description: prints Holberton using the function
 * Return: 0
 *
 */

int _strlen(char *s)
{
int a;
for (a = 0; s[a] != '\0'; ++a)
{
}
return (a);
}
